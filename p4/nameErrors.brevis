record Point (
    integer x;
    integer y;
);
boolean Point;

integer y;
record y(
	integer a;
	integer b;
);

record dupRecord (
	boolean b;
);

void main() {

	/// multiply declared struct
	record dupRecord dup;
	record dupRecord dup;

	/// invalid name of struct type
	record invalidName name;

	/// Dot-access of non struct type
	i.t = 12;

	/// RHS invalid struct field name
	struct1.i = 12;
}
